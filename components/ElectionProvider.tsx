"use client";import{createContext,useContext,useEffect,useState,ReactNode}from"react";import{Election}from"@/types/election";import{readElection,writeElection}from"@/lib/election/storage";const C=createContext<{election:Election|null;save:(e:Election)=>void;ready:boolean}>({election:null,save:()=>{},ready:false});export function ElectionProvider({children}:{children:ReactNode}){const[election,setElection]=useState<Election|null>(null);const[ready,setReady]=useState(false);useEffect(()=>{setElection(readElection());setReady(true);const f=()=>setElection(readElection());addEventListener("election-change",f);return()=>removeEventListener("election-change",f)},[]);const save=(e:Election)=>{writeElection(e);setElection(e)};return <C.Provider value={{election,save,ready}}>{children}</C.Provider>}export const useElection=()=>useContext(C);
