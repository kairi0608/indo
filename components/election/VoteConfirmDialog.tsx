import*as Dialog from"@radix-ui/react-dialog";import{Candidate}from"@/types/election";import{Button}from"@/components/ui/Button";
export function VoteConfirmDialog({candidate,onBack,onConfirm,busy}:{candidate:Candidate;onBack:()=>void;onConfirm:()=>void;busy:boolean}){return <Dialog.Root open onOpenChange={o=>!o&&onBack()}><Dialog.Portal><Dialog.Overlay className="overlay"/><Dialog.Content className="dialog"><Dialog.Title>この候補者に投票しますか？</Dialog.Title><div className="confirm-symbol">{candidate.symbol}</div><h2>{candidate.name}</h2><p>確定後は取り消せません。</p><div className="dialog-actions"><Button onClick={onBack} disabled={busy} className="secondary">戻る</Button><Button onClick={onConfirm} disabled={busy}>{busy?"記録中…":"この候補に投票する"}</Button></div></Dialog.Content></Dialog.Portal></Dialog.Root>}
