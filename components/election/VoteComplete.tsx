"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{CheckCircle2}from"lucide-react";import{freshSession,writeSession}from"@/lib/election/storage";import{Button}from"@/components/ui/Button";export function VoteComplete(){const router=useRouter();const[seconds,setSeconds]=useState(5);useEffect(()=>{const t=setInterval(()=>setSeconds(x=>Math.max(0,x-1)),1000);return()=>clearInterval(t)},[]);const next=()=>{writeSession(freshSession());router.replace("/vote")};return <div className="complete"><CheckCircle2 size={92}/><h1>投票が完了しました</h1><p>あなたの投票は記録されました。</p><div className="privacy-note">投票先はこの画面には表示されません</div><Button onClick={next} disabled={seconds>0}>次の投票者 {seconds>0&&`（${seconds}秒）`}</Button></div>}
